# Container panel

import { ComponentShowcase } from '../components/component-showcase';
import { ContainerPanel, Checkbox, Button } from '@eightshift/ui-components';
import { icons } from '@eightshift/ui-components/icons';

A perfect shell for options within the Block editor sidebar. Replaces the default WordPress `<PanelBody>` component.

<ComponentShowcase className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'>
	<ContainerPanel
		title='My block'
		className='es:bg-white'
	>
		<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
			<span>This is demo content</span>
		</div>
	</ContainerPanel>
</ComponentShowcase>

```jsx
<ContainerPanel title='My block'>
	<span>This is demo content</span>
</ContainerPanel>
```

## Highlighted props

For the complete list of props, use your IDE's autocomplete functionality.

### Closable

<ComponentShowcase className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'>
	<ContainerPanel
		title='My block'
		className='es:bg-white'
		closable
	>
		<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
			<span>This is demo content</span>
		</div>
	</ContainerPanel>
</ComponentShowcase>

```jsx
<ContainerPanel
	title='My block'
	// highlight-next-line
	closable
>
	<span>This is demo content</span>
</ContainerPanel>
```

### Use toggle

<ComponentShowcase className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'>
	{(data, setData) => (
		<ContainerPanel
			title='My block'
			className='es:bg-white'
			use={data}
			onUseChange={setData}
		>
			<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
				<span>This is demo content</span>
			</div>
		</ContainerPanel>
	)}
</ComponentShowcase>

```jsx
<ContainerPanel
	title='My block'
	// highlight-start
	use={value}
	onUseChange={(value) => setValue(value)}
	// highlight-end
>
	<span>This is demo content</span>
</ContainerPanel>
```

### Use toggle and closable

<ComponentShowcase className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'>
	{(data, setData) => (
		<ContainerPanel
			title='My block'
			className='es:bg-white'
			use={data}
			onUseChange={setData}
			closable
		>
			<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
				<span>This is demo content</span>
			</div>
		</ContainerPanel>
	)}
</ComponentShowcase>

```jsx
<ContainerPanel
	title='My block'
	// highlight-start
	use={value}
	onUseChange={(value) => setValue(value)}
	closable
	// highlight-end
>
	<span>This is demo content</span>
</ContainerPanel>
```

### Externally toggle use

<ComponentShowcase
	className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'
	preContent={(data, setData) => (
		<Checkbox
			checked={data}
			onChange={setData}
			label='Enabled'
		/>
	)}
	defaultValue={false}
>
	{(data) => (
		<ContainerPanel
			title='My block'
			className='es:bg-white'
			use={data}
			closable
		>
			<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
				<span>This is demo content</span>
			</div>
		</ContainerPanel>
	)}
</ComponentShowcase>

```jsx
<ContainerPanel
	title='My block'
	// highlight-start
	use={myUseValue}
	closable
	// highlight-end
>
	<span>This is demo content</span>
</ContainerPanel>
```

### Actions

<ComponentShowcase className='es:bg-gray-50 esd-hide-bg-on-hover es:transition'>
	{(data, setData) => (
		<ContainerPanel
			title='My block'
			className='es:bg-white'
			actions={
				<>
					<Button size='small'>Demo 1</Button>
					<Button size='small'>Demo 2</Button>
				</>
			}
			closable
		>
			<div className='es:w-full es:rounded-lg es:bg-gray-100 es:text-sm es:p-4 es:min-h-40'>
				<span>This is demo content</span>
			</div>
		</ContainerPanel>
	)}
</ComponentShowcase>

```jsx
<ContainerPanel
	title='My block'
	closable
	// highlight-start
	actions={
		<>
			<Button size='small'>Demo 1</Button>
			<Button size='small'>Demo 2</Button>
		</>
	}
	// highlight-end
>
	<span>This is demo content</span>
</ContainerPanel>
```
